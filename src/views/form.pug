div#display
    img.preview
form(action="/write" method="POST" enctype="multipart/form-data")
    p
        label(for='user') 유저
        input#user(type="text" value="zach" name="username")
    p
        label(for="fileInput") 이미지 업로드
        input#fileInput(type="file" name="image")
    p
        input(type="submit" value="업로드")